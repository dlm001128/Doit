<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <group
        android:id="@+id/sort_group"
        android:checkableBehavior="single">

        <item android:id="@+id/menu_sort_by_deadline"
            android:title="@string/sort_by_deadline"
            android:checked="true"/>

        <item android:id="@+id/menu_sort_by_project"
            android:title="@string/sort_by_project"
            android:checked="false" />
    </group>

    <group
        android:id="@+id/show_group"
        android:checkableBehavior="all">

        <item android:id="@+id/menu_show_finished"
            android:checkable="true"
            android:title="@string/show_finished"
            android:checked="false">
        </item>

        <item android:id="@+id/menu_show_hidden"
            android:checkable="true"
            android:title="@string/show_hidden"
            android:checked="false">
        </item>
    </group>

</menu>